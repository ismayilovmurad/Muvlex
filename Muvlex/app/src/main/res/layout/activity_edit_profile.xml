<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.martiandeveloper.muvlex.viewmodel.feed.EditProfileViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".view.feed.EditProfileActivity">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/activity_edit_profile_mainMTB"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:backgroundTint="@color/color_background"
            android:elevation="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:title="@string/app_name"
            app:titleTextColor="@color/color_supreme_text" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.cardview.widget.CardView
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:onClick="@{()->viewModel.onPictureIVClick()}"
                app:cardBackgroundColor="@android:color/transparent"
                app:cardCornerRadius="50dp"
                app:cardElevation="0dp">

                <ImageView
                    android:id="@+id/activity_edit_profile_posterIV"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/poster" />

            </androidx.cardview.widget.CardView>

            <EditText
                android:id="@+id/activity_edit_profile_bioET"
                style="@style/StyleRegularEditText"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="32dp"
                android:layout_marginTop="32dp"
                android:background="@drawable/edit_text_background"
                android:hint="@string/bio"
                android:imeOptions="actionDone"
                android:inputType="text"
                android:maxLength="50"
                android:padding="16dp"
                android:text="@={viewModel.bioETText}"
                tools:ignore="Autofill" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/activity_edit_profile_saveMBTN"
                style="@style/StyleSupremeMaterialButton"
                android:layout_marginHorizontal="32dp"
                android:layout_marginVertical="16dp"
                android:onClick="@{()->viewModel.onSaveMBTNClick()}"
                android:text="@string/save" />

            <ProgressBar
                android:id="@+id/activity_edit_profile_mainPB"
                android:layout_height="48dp"
                android:layout_width="48dp"
                style="@style/StyleSupremeProgressbar"/>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
